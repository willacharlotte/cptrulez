<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Quiz about testing principles">
    <title>Results</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css"
    />
  </head>
  <body class="container">
    <main class="container">
      <% const scorePercent = score.score / score.total * 100 %> <% if
      (scorePercent === 100) { %>
      <h1>You're a testing natural! 🎉</h1>
      <% } else if (scorePercent >= 50) { %>
      <h1>You're getting there. 🤓</h1>
      <% } else if (scorePercent > 0) { %>
      <h1>You may need to hit the books. 📚</h1>
      <% } else { %>
      <h1>You need serious help. 😵</h1>
      <% } %>

      <h2>You scored <%= score.score %> out of <%= score.total %></h2>
    </main>
    <a href="/" role="button">Play Again!</a>
  </body>
</html>
